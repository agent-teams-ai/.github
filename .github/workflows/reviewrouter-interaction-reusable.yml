name: Reusable ReviewRouter Interaction

on:
  workflow_call:
    inputs:
      discussion_mode:
        description: Enable ReviewRouter discussion replies.
        required: false
        type: string
        default: "off"
      discussion_max_per_pr:
        description: Maximum discussion replies per pull request.
        required: false
        type: string
        default: "20"
      discussion_max_per_thread:
        description: Maximum discussion replies per thread.
        required: false
        type: string
        default: "5"
      discussion_timeout_seconds:
        description: Discussion reply timeout in seconds.
        required: false
        type: string
        default: "60"
      codex_model:
        description: Codex model used for discussion replies.
        required: false
        type: string
        default: "gpt-5.5"
      codex_reasoning_effort:
        description: Codex reasoning effort used for discussion replies.
        required: false
        type: string
        default: "xhigh"
    secrets:
      REVIEWROUTER_CODEX_AUTH_JSON:
        description: Rotating Codex subscription authentication.
        required: false
      REVIEW_ROUTER_LEDGER_KEY:
        description: ReviewRouter memory ledger encryption key.
        required: false

permissions: {}

jobs:
  interaction:
    name: interaction
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    if: ${{ github.event_name == 'workflow_dispatch' || ((github.event_name == 'issue_comment' || github.event_name == 'pull_request_review_comment') && (github.event_name != 'issue_comment' || github.event.issue.pull_request) && github.event.comment.user.type != 'Bot') }}
    permissions:
      actions: write
      contents: read
      issues: read
      pull-requests: read
      id-token: write
    env:
      RR_RUNTIME_REF: "08f6bc1481fd284fa82adfa47cda05c76b161b00"
      REVIEWROUTER_API_URL: "https://api.reviewrouter.site"
      REVIEWROUTER_OIDC_AUDIENCE: "reviewrouter"
      REVIEWROUTER_RUNTIME_CONFIG_MODE: "oidc"
      REVIEWROUTER_STATIC_CONFIG_FALLBACK: "true"
      REVIEWROUTER_COMMENT_TOKEN_MODE: "app-oidc"
      CODEX_AUTH_JSON_PRESENT: ${{ secrets.REVIEWROUTER_CODEX_AUTH_JSON != '' && '1' || '0' }}
      REVIEW_ROUTER_REVIEW_WORKFLOW_FILE: "reviewrouter-codex.yml"
      REVIEW_ROUTER_MEMORY_ENABLED: "true"
      REVIEW_ROUTER_MEMORY_PROTOCOL_VERSION: "1"
      REVIEW_ROUTER_MEMORY_BUNDLE_ENDPOINT: "/api/action/v1/memory"
      REVIEW_ROUTER_MEMORY_CANDIDATE_ENDPOINT: "/api/action/v1/memory-candidates"
      REVIEW_ROUTER_MEMORY_COMMAND_ENDPOINT: "/api/action/v1/memory-commands"
    steps:
      - name: Checkout ReviewRouter interaction runtime
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          repository: 777genius/review-router
          ref: ${{ env.RR_RUNTIME_REF }}
          path: .reviewrouter-runtime
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38
        with:
          node-version: "24"

      - name: Preflight ReviewRouter interaction
        id: preflight
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REVIEW_ROUTER_MODE: "interaction-preflight"
          REVIEW_ROUTER_DISCUSSION_MODE: ${{ inputs.discussion_mode }}
        run: node .reviewrouter-runtime/dist/index.js

      - name: Install Codex CLI for discussion replies
        if: ${{ steps.preflight.outputs.needs_discussion == 'true' && env.CODEX_AUTH_JSON_PRESENT == '1' }}
        shell: bash
        run: npm install -g @openai/codex@0.141.0

      - name: Restore Codex subscription auth for discussion replies
        if: ${{ steps.preflight.outputs.needs_discussion == 'true' && env.CODEX_AUTH_JSON_PRESENT == '1' }}
        shell: bash
        env:
          CODEX_AUTH_JSON: ${{ secrets.REVIEWROUTER_CODEX_AUTH_JSON }}
        run: |
          set -euo pipefail
          if [ -z "${CODEX_AUTH_JSON:-}" ]; then
            echo "::error::REVIEWROUTER_CODEX_AUTH_JSON secret is missing. Re-run ReviewRouter Codex setup."
            exit 1
          fi
          export CODEX_HOME="${CODEX_HOME:-$HOME/.codex}"
          mkdir -p "$CODEX_HOME"
          chmod 700 "$CODEX_HOME"
          printf '%s' "$CODEX_AUTH_JSON" > "$CODEX_HOME/auth.json"
          chmod 600 "$CODEX_HOME/auth.json"

      - name: Run ReviewRouter interaction
        if: ${{ steps.preflight.outputs.should_run == 'true' }}
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          REVIEW_ROUTER_MODE: "interaction"
          REVIEW_ROUTER_LEDGER_KEY: ${{ secrets.REVIEW_ROUTER_LEDGER_KEY }}
          REVIEW_ROUTER_DISCUSSION_MODE: ${{ inputs.discussion_mode }}
          REVIEW_ROUTER_DISCUSSION_MAX_PER_PR: ${{ inputs.discussion_max_per_pr }}
          REVIEW_ROUTER_DISCUSSION_MAX_PER_THREAD: ${{ inputs.discussion_max_per_thread }}
          REVIEW_ROUTER_DISCUSSION_TIMEOUT_SECONDS: ${{ inputs.discussion_timeout_seconds }}
          CODEX_MODEL: ${{ inputs.codex_model }}
          CODEX_REASONING_EFFORT: ${{ inputs.codex_reasoning_effort }}
        run: node .reviewrouter-runtime/dist/index.js
